import { FastifyInstance } from "fastify";

export function socialProjectsUnprotectedRoutes(app: FastifyInstance) {
  // app.post("/example", { preHandler: [
  //   authMiddleware
  // ] })
  // app.post("/socialProjects", authController.login);
}
